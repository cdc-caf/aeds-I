#include <stdio.h>
#include <stdlib.h>

int main() {

    int **pnt_pnt_vetor;
    int N = 10;
    int i;
    pnt_pnt_vetor = (int**) malloc(N * sizeof(int*));

    /* 
        Complete o código para gerar uma matriz onde a primeira Linha tem 1 item, a segunda tem 2, ..., a N-ésima linha tem N itens.
    */

    for(i=1; i<=N; i++) {
        
    }

    return 0;
}
