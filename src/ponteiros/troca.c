#include <stdio.h>

/* 
    Complete a função com o código e com os parâmetros necessários para trocar os valores de a e b.
*/
int troca() {
      
}

int main() {
    int a = 10;
    int b = 15;

    printf("\nAntes: a = %d e b = %d\n", a, b);
    troca();
    printf("\nDepois: a = %d e b = %d\n", a, b);
}
